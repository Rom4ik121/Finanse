name: flet_local_auth
description: Finanse biometric auth bridge (local_auth)
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.2.3 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  local_auth: ^2.3.0
  flet:
    path: ../../../../../../../packages/flet

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
